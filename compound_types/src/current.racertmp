fn main() {
	let x = (1i, "hello"); // tuple
	let x1 : (int, &str) = (1, "hello"); // with type annotated

	let (a, b, c) = (1i, 2i, 3i);
	println!("a is {}", a);

	let d = (1i, 2i ,3i);
	let e = (2i, 3i, 4i);

	 if d == e { 
	 	println!("yes")
	 } else { 
	 	println!("no")
	 }

	 let (f, g) = next_two(5i);
	 println!("f, g = {}, {}", f, g);
}

fn next_two(x: int) -> (int, int) { (x + 1i, x + 2i) }